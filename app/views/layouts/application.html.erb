<!DOCTYPE html>
<html>
  <head>
    <title>CctsGroup1</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Khairul : Bootstrap cdn link for css -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">

    <!--stylesheet files-->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
      
    <!-- Awesome Icons-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css" integrity="sha384-..." crossorigin="anonymous">
    
    <%= javascript_include_tag "//www.google.com/jsapi", "chartkick"%>
    <%= stylesheet_link_tag 'https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css' %>

    <%= yield :javascript%>
    <!--JS for charts-->
    <%= javascript_include_tag "//www.google.com/jsapi", "chartkick" %>

    <!-- Add Bootstrap JS script -->
    
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="big_con">
      <div class="left_container ">
          <%= render 'shared/header' unless browser.device.mobile? %>
        <div class = "left_container_content">
          <%= render 'shared/miniDash' if (current_user.is_a?(Staff) || current_user.is_a?(Admin)) && !browser.device.mobile? %>
        </div>

        <%= render 'shared/sideBar' if current_user.is_a?(Admin) && !browser.device.mobile? %>

        <p class="alert"><%= alert %></p>
        <div class='mainbody' >
          <%= yield %>
        </div>
      </div>
      <div class="right_container">
        <%= render 'shared/userCard' unless browser.device.mobile? %>
        <%= render 'shared/calendar' unless browser.device.mobile? %>
        <%= render 'shared/upcomingEvents' unless browser.device.mobile?%>
        
      </div>
      
    </div>
  </body>
</html>


