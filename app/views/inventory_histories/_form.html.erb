<%= form_with(model: inventory_history) do |form| %>
  <% if inventory_history.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(inventory_history.errors.count, "error") %> prohibited this inventory_history from being saved:</h2>

      <ul>
        <% inventory_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<table class="table table-striped mx-auto">
  <tbody>
    <tr>
      <th scope="row" class="col-6">Equipment Name</th>
      <td class="col-6 text-center"><%= @inventory.name %></td>
    </tr>
    <tr>
      <th scope="row" class="col-6">Description</th>
      <td class="col-6 text-center"><%= @inventory.description %></td>
    </tr>
    <tr>
      <th scope="row" class="col-6">Quantity</th>
      <td class="col-6 text-center">
        <%= form.number_field :quantity, value: @inventory.quantity, style: "width: 5em; text-align: center;" %>
      </td>
    </tr>
  </tbody>
</table>



  <div>
    <%= form.label :inventory_id, style: "display: none" %>
    <%= form.hidden_field :inventory_id, value: @inventory.id %>
  </div>

 <div>
  <!--<%= form.label :quantity, "Current quantity", style: "display: block" %>-->
  <!--<%= form.number_field :quantity, value: @inventory.quantity, readonly: true %>-->
</div>

  <div>
    <%= form.submit "Update Inventory Quantity", class: "btn btn-primary btn-block" %>
  </div>
<% end %>